{
    "name": "chatsapp-frontend",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "mkdir -p dist && cp index.html dist/ && cp -r public/* dist/ 2>/dev/null || true",
        "preview": "vite preview"
    },
    "dependencies": {
        "react": "^18.2.0",
        "react-dom": "^18.2.0",
        "socket.io-client": "^4.5.4"
    },
    "devDependencies": {
        "@vitejs/plugin-react": "^3.1.0",
        "terser": "^5.46.0",
        "vite": "^4.5.0"
    }
}
